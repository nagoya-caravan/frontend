import { Box, Typography } from "@mui/material";

const Calendar = () => {
  return (
    <Box>
      <Typography>Calendar</Typography>
      <Calendar />
    </Box>
  );
};

export default Calendar;
